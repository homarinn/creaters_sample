class Api::MessagesController < ApplicationController
  def index
    # message = Message.find(params[:message_id])
    # @messages = Message.where("id > ? and message_room_id = ?", message.id, message.message_room_id)
  end
end